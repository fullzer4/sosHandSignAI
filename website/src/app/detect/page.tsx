import { NextComponentType } from "next"
import Link from 'next/link';

const Detect: NextComponentType = () => {
    return (
      <main>
        <Link href="/">ir para</Link>
      </main>
    )
}

export default Detect